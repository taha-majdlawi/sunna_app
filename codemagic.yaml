workflows:
  ios-release:
    name: Sunaa App iOS Release
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
      groups:
        - app_store_credentials 
    scripts:
      - name: Set up code signing
        script: | 
          keychain initialize
          echo "$APP_STORE_CONNECT_KEY" > /tmp/apple_key.p8
          
          # جلب الشهادات وملفات التعريف (Provisioning Profiles)
          app-store-connect fetch-signing-files "com.example.sunaaApp" \
            --issuer-id "$APP_STORE_CONNECT_ISSUER_ID" \
            --key-id "$APP_STORE_CONNECT_KEY_ID" \
            --p8-key /tmp/apple_key.p8 \
            --type IOS_APP_STORE \
            --create
            
          keychain add-certificates
          
          # تثبيت ملفات التعريف في المكان الصحيح داخل الماك
          xcode-project use-profiles
          
          rm /tmp/apple_key.p8
      - name: Get Flutter packages
        script: flutter pub get
      - name: Build iOS IPA
        script: | 
          # التعديل الجوهري: إجبار Xcode على التوقيع اليدوي باستخدام ما تم جليه
          flutter build ipa --release \
            --export-method app-store \
            --build-number=18 \
            --code-sign-style=Manual
    artifacts:
      - build/ios/ipa/*.ipa